// export const userDisplayName = (useruid) => {
//   const userDetailsDoc = `userDetails@${useruid}`;
//   if (props.users[userDetailsDoc])
//     return props.users[userDetailsDoc].displayName;
//   else {
//     return props.loadUserDetails({ useruid });
//   }
// };
